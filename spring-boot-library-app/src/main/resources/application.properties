spring.application.name=spring-boot-library
spring.datasource.url=jdbc:mysql://localhost:3306/reactlibrarydatabase?user=root&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=root

spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

spring.data.rest.base-path=/api

spring.hateoas.use-hal-as-default-json-media-type=false

okta.oauth2.client-id=0oalr73k0sZQ7G5OL5d7
okta.oauth2.issuer=https://dev-64381758.okta.com/oauth2/default

stripe.key.secret=${STRIPE_SECRET_KEY}  (activate this line)

#####
#
# HTTPS configuration
#
#####
# Server web port
server.port=8443
# Enable HTTPS support (only accept HTTPS requests)
server.ssl.enabled=true
# Alias that identifies the key in the key store
server.ssl.key-alias=luv2code
# Keystore location
server.ssl.key-store=classpath:luv2code-keystore.p12
# Keystore password
server.ssl.key-store-password=secret
# Keystore format
server.ssl.key-store-type=PKCS12

